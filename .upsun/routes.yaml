# 确保URL形如https://...和www....的请求，都将被路由到同一个地方

routes:

  "https://{default}/":
    type: upstream
    upstream: "ll_project:http"

  "https://www.{default}/":
    type: redirect
    to: "https://{default}/"