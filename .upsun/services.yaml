# Each service listed will be deployed in its own container as part of your
#   Platform.sh project.

services:
  db:
    type: postgresql:16
    # disk: "1024"    - services.db.disk: disk configuration is not supported